// cytoscape-layout-base stub to keep Dash asset ordering intact.
// The bundled fcose/euler layouts already ship with their layout base code,
// but Dash loads this file first, so it must contain valid JavaScript.
(function () {
  // Nothing to register; this placeholder prevents ReferenceError during asset load.
})();
